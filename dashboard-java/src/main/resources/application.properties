# Path to requirements.csv. If empty, uses parent dir: ../requirements.csv (when run from dashboard-java).
# Override with: VARMINER_CSV_PATH=/absolute/path/to/requirements.csv
varminer.requirements.csv-path=${VARMINER_CSV_PATH:}

server.port=8080
